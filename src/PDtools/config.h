#ifndef CONFIG_H
#define CONFIG_H

#include <string>
#include <iostream>
#include <armadillo>
#include <map>
#include <unordered_map>
#include <vector>

namespace PDtools
{
using std::cout;
using std::endl;
using std::cerr;
using std::map;
using std::unordered_map;
using std::vector;
using std::string;
using std::string;
using arma::mat;
using arma::ivec;
using arma::vec;
using arma::vec2;
using arma::vec3;
using arma::zeros;
}

#endif // CONFIG_H
